# Copy to terraform.tfvars (gitignored) and fill in — OR skip this file and set
# the env var instead:  export TF_VAR_gemini_api_key="..."
# (The `make tf-apply` target reads the key straight from your .env for you.)

gemini_api_key = "your-gemini-key-here"

# Optional overrides (defaults shown):
# cluster_name = "scholarrag"
# api_replicas = 1
