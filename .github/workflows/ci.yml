name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: lint · type-check · test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        # No --all-extras: keeps CI free of heavy optional deps (torch, etc.).
        # Tests use the dependency-free FakeEmbedder + LocalVectorStore.
        run: uv sync

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Ruff lint
        run: uv run ruff check .

      - name: Mypy
        run: uv run mypy src tests

      - name: Pytest
        # No Pinecone/LLM keys in CI → LocalVectorStore, FakeEmbedder, no live calls.
        env:
          ENVIRONMENT: ci
          VECTOR_STORE: local
          EMBEDDING_PROVIDER: fake
        run: uv run pytest
